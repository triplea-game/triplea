/**
 * Provides support for the Java object serialization persistence layer for model objects exported by the
 * {@code games.strategy.*} packages.
 */
package games.strategy.internal.persistence.serializable;
