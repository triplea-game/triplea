package games.strategy.engine.delegate;

/**
 * A section of code that implements game logic and can be invoked by the
 * IGamePlayer through the IPlayerBridge at any time.
 */
public interface IPersistentDelegate {
}
