#!/bin/bash

set -e

scriptDir=$(dirname "$0")
"$scriptDir/../../gradlew" :game-app:game-headed:check

"$scriptDir/.build/code-convention-checks/run"
"$scriptDir/.build/find-orphaned-tests"
"$scriptDir/.build/check-links"

