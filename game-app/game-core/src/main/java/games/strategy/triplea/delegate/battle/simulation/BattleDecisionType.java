package games.strategy.triplea.delegate.battle.simulation;

/** Decision currently required before a simulated battle can continue. */
public enum BattleDecisionType {
  NONE,
  SELECT_CASUALTIES,
  RETREAT,
  SUBMERGE
}
