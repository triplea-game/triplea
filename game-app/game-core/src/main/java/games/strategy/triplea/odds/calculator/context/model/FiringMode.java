package games.strategy.triplea.odds.calculator.context.model;

/** Per firing group: which counts feed the roll — live working map vs exchange-start snapshot. */
public enum FiringMode {
  IMMEDIATE,
  DEFERRED
}
