package games.strategy.ui;

/** A listener that is notified of changes to instances of {@link ScrollableTextField}. */
public interface ScrollableTextFieldListener {
  void changedValue(ScrollableTextField field);
}
