package games.strategy.triplea.delegate;

import games.strategy.engine.delegate.IDelegateBridge;

/**
 * Logic for initializing a Two If By Sea game.
 *
 * @deprecated Required for map compatibility. Remove upon next map-incompatible release.
 */
@Deprecated
public class TwoIfBySeaInitDelegate extends InitializationDelegate {
  @Override
  protected void init(final IDelegateBridge bridge) {
    super.init(bridge);
  }
}
